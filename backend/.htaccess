# GoDaddy Python App Configuration
# IMPORTANT: Update PassengerAppRoot with your actual Application Root path

# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION BEGIN
PassengerAppRoot "/home/g17po2g810k9/public_html/sbbs-tech-admin/backend"
PassengerBaseURI "/"
# PassengerPython - Leave commented out if GoDaddy auto-detects Python
# If you need to specify, uncomment and set the path from Python App settings
PassengerPython "/usr/bin/python3"
PassengerAppLogFile "/home/g17po2g810k9/logs/sbbs-tech-admin-backend.log"
# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION END

# Turn off directory index
Options -Indexes

# Enable rewrite engine for React Router (SPA routing)
RewriteEngine On
RewriteBase /

# Don't rewrite files that exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Don't rewrite API calls
RewriteCond %{REQUEST_URI} !^/api/

# Rewrite everything else to index.html for React Router
RewriteRule . /index.html [L]

