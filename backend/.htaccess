# GoDaddy Python App Configuration
# IMPORTANT: This file must be in Application Root with permissions 644

# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION BEGIN
PassengerAppRoot "/home/g17po2g810k9/public_html/sbbs-tech-admin/backend"
PassengerBaseURI "/"
PassengerPython "/usr/bin/python3"
PassengerAppLogFile "/home/g17po2g810k9/logs/sbbs-tech-admin-backend.log"
# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION END

# Security: Turn off directory index
Options -Indexes

# Enable rewrite engine for React Router (SPA routing)
RewriteEngine On
RewriteBase /

# Don't rewrite if file exists
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Don't rewrite if directory exists
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Don't rewrite API calls (let FastAPI handle them)
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^ - [L]

# Rewrite everything else to index.html for React Router
RewriteRule ^ /index.html [L]

